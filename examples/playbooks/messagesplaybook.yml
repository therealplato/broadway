id: messagesplaybook
name: Hello Word Playbook
vars: 
  - word
  - bird
tasks:
  - name: Deployment Task
    pod_manifest: hello-pod
messages:
  created: "custom created {{.playbook_id}}/{{.id}}"
  deployed: "custom deployed {{.playbook_id}}/{{.id}}"
